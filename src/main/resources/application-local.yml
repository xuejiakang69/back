# 本地开发配置
server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://47.103.116.90:3306/shop_test?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: shop_test
    password: 1234567890
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql
